import { ScrollRtlDirective } from './scroll-rtl.directive';

describe('ScrollRtlDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollRtlDirective();
    expect(directive).toBeTruthy();
  });
});
