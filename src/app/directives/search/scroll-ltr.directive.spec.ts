import { ScrollLtrDirective } from './scroll-ltr.directive';

describe('ScrollLtrDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollLtrDirective();
    expect(directive).toBeTruthy();
  });
});
