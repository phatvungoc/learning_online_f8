import { HighlightPageDirective } from './highlight-page.directive';

describe('HighlightPageDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightPageDirective();
    expect(directive).toBeTruthy();
  });
});
