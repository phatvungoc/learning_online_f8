import { ToggleAltSidebarDirective } from './toggle-alt-sidebar.directive';

describe('ToggleAltSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleAltSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
