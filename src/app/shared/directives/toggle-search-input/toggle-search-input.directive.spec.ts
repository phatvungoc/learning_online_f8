import { ToggleSearchInputDirective } from './toggle-search-input.directive';

describe('ToggleSearchInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleSearchInputDirective();
    expect(directive).toBeTruthy();
  });
});
